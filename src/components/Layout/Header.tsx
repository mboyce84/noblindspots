import {Download,ArrowUpRight} from 'lucide-react';
import {Link} from 'react-router-dom';
export default function Header({title,subtitle,showExport,onExport}:{title:string;subtitle?:string;showExport?:boolean;onExport?:()=>void}){return <header className="app-header"><div><span className="header-breadcrumb">WORKSPACE <span>/</span> {title.toUpperCase()}</span><h1>{title}</h1>{subtitle&&<p>{subtitle}</p>}</div><div className="header-actions">{showExport&&<button className="button white" onClick={onExport}><Download size={16}/>Export</button>}<Link to="/imports" className="button dark">Import data <ArrowUpRight size={16}/></Link></div></header>}