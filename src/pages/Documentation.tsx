import {Link} from 'react-router-dom';
import Layout from '../components/Layout/Layout';
export default function Documentation(){return <Layout title="Workspace guide" subtitle="How to get trustworthy numbers into NoBlindSpots."><section className="panel guide-content"><h2>1. Start in My business</h2><p>Use the workspace selector to switch from sample data to your empty business workspace. These stores are separate. Each signed-in account owns its own data.</p><h2>2. Import the source of truth</h2><p>Start with GHL contacts, then add appointments, opportunities, and processor payments. Import CSVs with a stable source ID and YYYY-MM-DD dates. Amounts are USD dollars; the engine stores integer cents. Re-imports update records with the same source, type, and ID.</p><h2>3. Resolve exceptions</h2><p>Customer matching uses GHL contact IDs first, then an unambiguous email. Unmatched payments stay in cash totals but lack attribution. Review possible cross-source duplicates before excluding either record. Exclusions are reversible and logged.</p><h2>4. Complete your costs</h2><p>Import ad spend, commissions, delivery expenses, and overhead. Cash collected is successful payments plus negative refund records. Processing fees are subtracted once in contribution margin. Costs without an offer stay visible as Unallocated; overhead without an offer is allocated across positive net offer collections.</p><h2>5. Verify before deciding</h2><p>Click headline numbers to inspect calculation rules and source rows. Filters affect both charts and headlines. Contracted value and cash refer to different events, so their difference is not a receivables balance.</p><h2>6. Keep the human context</h2><p>Team roster entries are reporting profiles, not sign-in invitations. Owners can submit EOD forms for those profiles. Manual payment reports never get added to imported transactions. Team goals use manually reported progress.</p><h2>Current boundaries</h2><p>This version supports 500 rows per import, 1 MB CSV files, 10,000 records per workspace, USD, and the coaching business template. Automatic GHL/Stripe sync, billing, client sharing, additional templates, and a custom metric builder require a later release. No notifications or payment retries are sent by this app.</p><Link className="button dark" to="/imports">Import your first file</Link></section></Layout>;}
