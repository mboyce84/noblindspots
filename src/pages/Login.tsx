import {Link,Navigate} from 'react-router-dom';
import {ArrowRight,Eye,ShieldCheck} from 'lucide-react';
import {useAuth} from '../context/AuthContext';
export default function Login(){const{user,isLoading,error,reload}=useAuth();if(user)return <Navigate to="/dashboard" replace/>;
 return <main className="login-page"><Link to="/" className="brand"><Eye/>NoBlindSpots.</Link><div className="login-card"><span className="eyebrow">YOUR PRIVATE WORKSPACE</span><h1>A clearer view starts here.</h1><p>Open your sample workspace or bring your own exports. Your business data stays behind sign-in.</p>{error&&<p role="alert" className="notice warning">{error}</p>}<a href="/signin-with-chatgpt?return_to=/dashboard" target="_top" className="button dark">Sign in with ChatGPT <ArrowRight size={18}/></a><button className="button white" onClick={()=>void reload()} disabled={isLoading}>{isLoading?'Opening workspace…':'I’m signed in — open workspace'}</button><div className="login-note"><ShieldCheck size={18}/> Access is managed by your private app.</div></div><Link to="/" className="text-link">Back to NoBlindSpots</Link></main>;
}