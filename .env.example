# The hosted app uses Sites identity and its managed DB binding.
# No credentials are bundled into browser JavaScript.
# The local API runs on loopback and uses an explicitly local development identity.
# Legacy Supabase migration files are retained as design references.
# Native webhooks and external integrations are not active in this version.
VITE_WAITLIST_MODE=proxy
